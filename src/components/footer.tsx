// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as elements from 'typed-html';
import { Component } from '../types';

export const Footer: Component = () => (
    <section class="flex flex-row gap-2 items-center justify-between py-1 px-2">
        <input class="grow" type="text" name="" id="" placeholder="type..." />
        <button>send</button>
    </section>
);
